[env:pic16f876a]
platform = pic8bit
board = pic16f876a

; 4MHz crystal configuration
board_build.f_cpu = 4000000L

; Upload protocol (adapt to your hardware)
upload_protocol = pickit3
; upload_protocol = pickit2

; Compilation flags
build_flags = 
    -DDEBUG=1
    -Wall

; Library dependencies
lib_deps = 

; Serial port configuration (optional)
monitor_speed = 9600
monitor_port = COM3

